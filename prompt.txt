You are a senior MERN stack engineer.

I have an OLX-style project with separate repositories:

Backend Repository:
https://github.com/Attaint54/back-end-olx.git

Frontend Repository:
https://github.com/Attaint54/olx-cloi.git

My GitHub username is Attaint54.

Your task is to fully integrate MongoDB Atlas into the project and replace any mock, temporary, or in-memory data storage with MongoDB.

Requirements:

1. First analyze the entire backend codebase and understand its structure before making changes.

2. Install and configure:
   - mongoose
   - dotenv
   - bcryptjs (if authentication exists)
   - any other necessary dependencies

3. Create a production-ready MongoDB connection setup:
   - config/db.js
   - environment variable support
   - proper connection error handling
   - server should only start after successful MongoDB connection

4. Create clean architecture:
   - config/
   - models/
   - controllers/
   - routes/
   - middleware/
   - utils/

5. Create or update MongoDB models:
   - User
   - Listing/Product
   - Category (if applicable)
   - Any additional models required by the project

6. Implement complete CRUD operations:
   - Create listing
   - Get all listings
   - Get listing by ID
   - Update listing
   - Delete listing

7. Add:
   - Validation
   - Proper HTTP status codes
   - Error handling middleware
   - Async/await patterns
   - Clean controller logic

8. Authentication:
   - Store users in MongoDB
   - Hash passwords using bcryptjs
   - Never store plain text passwords
   - Update existing auth logic if needed

9. Environment Variables:
   Create:
   - .env.example

   Required variables:
   PORT=
   MONGODB_URI=
   JWT_SECRET=

10. Update all existing routes and controllers to use MongoDB Atlas.

11. Keep existing functionality working:
   - Login
   - Signup
   - Listings
   - User profiles
   - Search
   - Any existing OLX features

12. Frontend Integration:
   - Verify all API endpoints used by the frontend
   - Ensure frontend requests continue working
   - Fix endpoint mismatches if found

13. Code Quality:
   - Follow Express best practices
   - Remove dead code
   - Add comments where necessary
   - Avoid code duplication

14. At completion provide:
   - List of files created
   - List of files modified
   - MongoDB Atlas setup instructions
   - Commands required to run the project
   - Testing steps

15. Do NOT give placeholders or pseudo-code.
   Generate complete working code for every modified file.

16. Do NOT break existing functionality.

17. If a feature is missing from the repositories but required for MongoDB integration, implement it properly.

MongoDB Atlas connection string will be provided through:

MONGODB_URI=<atlas_connection_string>

Work directly on the repository code and make all required changes for a fully functional MongoDB-powered OLX application.